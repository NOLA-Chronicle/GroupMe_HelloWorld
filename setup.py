from setuptools import setup

setup(
   name='GroupMe_HelloWorld',
   version='1.0.0',
   description='Beginning groupme bot',
   author='Ryan Johnson',
   author_email='Chronicle.codes@gmail.com',
   packages=['GroupMe_HelloWorld'],  #same as name
   install_requires=['time'], #external packages as dependencies
)
